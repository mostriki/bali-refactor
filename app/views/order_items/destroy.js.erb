$('#deleteFromCart<%= @item.id %>').remove();
$('.total').html('<%= number_to_currency current_order.total_price %>');
$('.navQuantity').html('<%= link_to "Cart | Total items: #{ cart_items }", cart_path %>');
